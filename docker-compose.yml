services:
  wire-team-bot:
    # Pull the latest image built by CI from GitHub Container Registry.
    # To build locally instead: replace `image:` with `build: .`
    image: ghcr.io/adamlow-wire/wire-team-bot:latest
    container_name: wire-team-bot
    restart: unless-stopped
    env_file:
      # Copy .env.example → .env on the server and fill in real values.
      # This file is never committed to the repo.
      - .env
    environment:
      # Override DATABASE_URL to point at the compose postgres service.
      DATABASE_URL: postgres://wirebot:wirebot@postgres:5432/wire_team_bot
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      # Wire SDK store (SQLite + CoreCrypto keystore) — must persist across restarts.
      # Losing this means the bot loses all E2EE session state.
      # NOTE: a volume created by the pre-2026-09 fork-based image is not compatible;
      # start the official-SDK image on a fresh volume (see README.md, Official SDK cutover).
      - wire-team-bot-crypto:/app/storage

  postgres:
    image: pgvector/pgvector:pg16
    container_name: wire-team-bot-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: wirebot
      POSTGRES_PASSWORD: wirebot
      POSTGRES_DB: wire_team_bot
    volumes:
      - wire-team-bot-db:/var/lib/postgresql/data
    # Bind to localhost only — not reachable from outside the host.
    ports:
      - "127.0.0.1:5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U wirebot -d wire_team_bot"]
      interval: 5s
      timeout: 5s
      retries: 10

  # ── Optional: local Ollama for on-premises inference ─────────────────────
  # Uncomment this service and set WIRE_TEAM_BOT_LLM_BASE_URL=http://ollama:11434/v1
  # in your .env if you want all LLM calls to stay on the host.
  #
  # ollama:
  #   image: ollama/ollama:latest
  #   container_name: wire-team-bot-ollama
  #   restart: unless-stopped
  #   volumes:
  #     - wire-team-bot-ollama:/root/.ollama
  #   ports:
  #     - "127.0.0.1:11434:11434"
  #   # NVIDIA GPU support — uncomment if available:
  #   # deploy:
  #   #   resources:
  #   #     reservations:
  #   #       devices:
  #   #         - driver: nvidia
  #   #           count: all
  #   #           capabilities: [gpu]

volumes:
  wire-team-bot-db:
  wire-team-bot-crypto:
  # wire-team-bot-ollama:
